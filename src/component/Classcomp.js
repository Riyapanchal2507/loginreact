import React, { component } from "react";

export default class Classcomp extends component {
        render() {
            return (
                <h1>hello</h1>
            )
        }
    }